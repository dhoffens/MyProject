<h1> Users </h1>

<h2> Trustworthy salesmen: </h2>

<ul>
	<% @users.each do |user| %>
		<li>
		<% if user.name.present? %>  -
			<%= user.name %>  -
		<% end %>
		
		<% if user.email.present? %> 
			<%= user.email %> 
		<% end %>
		
		</li>
	<% end %>
</ul>